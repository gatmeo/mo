\qitem{%
    Let $a, b, c$ be positive integers with $0 < a, b, c < 11$. If $a, b, $ and $c$ satisfy \begin{alignat*}{1} 3a+b+c&\equiv abc\pmod{11} \\ a+3b+c&\equiv 2abc\pmod{11} \\ a+b+3c&\equiv 4abc\pmod{11} \end{alignat*}then find the sum of all possible values of $abc$. 
    }{%
    <++>
    }{%
    <++>
}

